# gdlc

An attested Claude Code plugin marketplace for the GitHub SDLC planning domain
(`github-sdlc-plugins`). Scaffolding in progress — see
`.claude-plugin/marketplace.json` and `plugins/` as they land.
